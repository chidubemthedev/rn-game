import React from "react";
import { StyleSheet, Text, View } from "react-native";

type Props = {};

const GameOverSvreen = (props: Props) => {
  return (
    <View>
      <Text>GameOverSvreen</Text>
    </View>
  );
};

export default GameOverSvreen;

const styles = StyleSheet.create({});
